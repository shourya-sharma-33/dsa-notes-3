you are given a positive integer n u have to find 4 int a,b,c,d 

so that 

a+b+c+d=n and gcd(a,b)=lcm(c, d)

basicaly what can we do is we can take

a = n-3
b=c=d=1

these goofy ahh tricks are more so a figure out thing

```cpp
void solve(){
    int n;
    cin>>n;
    cout<<n-1<" 1 1 1\n"
}
```