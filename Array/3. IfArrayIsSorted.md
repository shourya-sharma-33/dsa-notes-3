basically we can loop and see if a[i+1] < a[i] we return false

simple and sweet

```cpp
int checkSorted(int n, vector<int> a)
{
    for (int i = 1; i<n; i++)
    {
        if(a[i]>=a[i-1])
        {

        }
        else
        {
            return false;
        }
    }
}
```
